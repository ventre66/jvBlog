{% extends 'base.html.twig' %}

{% block title %}JvBlog{% endblock %}

{% block body %}
    {{ form_start(form)}}
    {{ form_row(form.name) }}
    <button type="submit" class="btn btn-success">valider</button>
    {{ form_end(form)}}
{% endblock %}
